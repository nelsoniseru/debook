import { CreateInteractionDto } from './create-interaction.dto';

describe('CreateInteractionDto', () => {
  it('should be defined', () => {
    expect(new CreateInteractionDto()).toBeDefined();
  });
});
